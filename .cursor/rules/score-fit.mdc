---
description: Score how well the user's master profile fits a specific job — an ATS-style match percentage with matched/missing keywords, strengths, gaps, and a clear apply-or-skip recommendation. Optionally writes a structured sidecar that tailor-resume consumes. Use to evaluate a single job (or a few) against the user's profile before they spend effort applying — typically on the top results from find-jobs, or any job description the user pastes.
alwaysApply: false
---

# score-fit

This task maps to the **`score-fit`** skill in this repo. Read [`skills/score-fit/SKILL.md`](../../skills/score-fit/SKILL.md) and follow it exactly — it is the single source of truth for this workflow.

Before producing any output, obey the shared rules in [`skills/_shared/RULES.md`](../../skills/_shared/RULES.md) (never fabricate, never echo the `context:` block, region-pack-first, no emoji, known-info gate).
