---
description: Build or update the JobClaw master profile — the single file every other job-hunt skill reads. Ingests a resume (extracting every embedded URL), LinkedIn PDF, exported AI-memory, and links, then asks only for what's missing in one batched, optional gap prompt. Captures career-goal context for downstream skills. Use at the start of a job hunt, when onboarding a new user, when the user shares a resume/CV/LinkedIn export, or whenever profile/master-profile.md is missing or stale.
alwaysApply: false
---

# build-profile

This task maps to the **`build-profile`** skill in this repo. Read [`skills/build-profile/SKILL.md`](../../skills/build-profile/SKILL.md) and follow it exactly — it is the single source of truth for this workflow.

Before producing any output, obey the shared rules in [`skills/_shared/RULES.md`](../../skills/_shared/RULES.md) (never fabricate, never echo the `context:` block, region-pack-first, no emoji, known-info gate).
