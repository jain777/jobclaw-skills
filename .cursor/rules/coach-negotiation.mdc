---
description: Coach a counter-offer — leverage analysis, floor / target / walk-away ranges per comp component, and a draft counter email the human sends (never auto-send). Reads companies/<slug>.json for market comp when present; otherwise asks for an inline range or runs a thin WebSearch. Region pack drives currency / structure (USD base+equity vs INR LPA CTC fixed+variable). Use after an offer arrives (or when preparing a counter). The reply-accept (offer) draft-reply template should have already bought time before this runs.
alwaysApply: false
---

# coach-negotiation

This task maps to the **`coach-negotiation`** skill in this repo. Read [`skills/coach-negotiation/SKILL.md`](../../skills/coach-negotiation/SKILL.md) and follow it exactly — it is the single source of truth for this workflow.

Before producing any output, obey the shared rules in [`skills/_shared/RULES.md`](../../skills/_shared/RULES.md) (never fabricate, never echo the `context:` block, region-pack-first, no emoji, known-info gate).
